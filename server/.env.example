DATABASE_URL=postgres://wager:wager@localhost:5432/credit_wager

# Access-token signing key. No default: the server refuses to boot without it.
# Generate one with: openssl rand -base64 48
JWT_SECRET=
